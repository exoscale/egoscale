package egoscale

// Version of the library
const Version = "0.21.0"

const (
	computeAPIVersion = "v1"
	// ComputeAPIEndpoint is the Exoscale Compute endpoint with the latest API version.
	ComputeAPIEndpoint = "https://api.exoscale.com/" + computeAPIVersion

	// DNSAPIEndpoint is the Exoscale API DNS endpoint.
	DNSAPIEndpoint = "https://api.exoscale.com/dns"
)
